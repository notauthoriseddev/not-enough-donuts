package jmd.notenoughdonuts

import net.fabricmc.api.ModInitializer

object NotEnoughDonuts : ModInitializer {
    const val MOD_ID = "not-enough-donuts"

    override fun onInitialize() {
        println("Not Enough Donuts mod initialized!")
    }
}
